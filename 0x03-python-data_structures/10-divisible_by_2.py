#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    newList = []
    for i in my_list:
        if (my_list[i] % 2 == 0)
            return ("{:d} is divisible by 2".format(i))
        else:
            return ("{:d} is not divisible by 2".format(i))


